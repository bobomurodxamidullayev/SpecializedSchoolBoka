/**
 * Base URL for all backend API calls.
 * Points directly to the backend Vercel deployment to avoid
 * frontend rewrite issues (HTML returned instead of JSON).
 */
export const API_BASE = "https://boka-school-api-server.vercel.app";
